deployment:
  tasks:
    - export DEPLOYPATH=/home/theodora/billberryblue.com/

    - /usr/bin/npm install

    - /usr/bin/npm run build

    - /bin/rm -rf $DEPLOYPATH/*

    - /bin/cp -R out/* $DEPLOYPATH
